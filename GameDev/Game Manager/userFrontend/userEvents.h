//Making Events/Update Loops follows this pattern.
//makeEvent(1)	//makeEvent(event_id) automatically checks if the window has been closed.
//	doStuff
//makeUpdate    //Update automatically updates container graphics on its own.
//  doStuff
//endEvent
//
//In order to move from one scene to another simply set currentScene to the value that you're changing to.
